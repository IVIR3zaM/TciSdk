repositories{
  jcenter()
  flatDir {
      dirs 'libs'
   }
}

dependencies {
   compile(name:'TciPaymentSDK', ext:'aar')
   compile 'com.github.bumptech.glide:glide:3.7.0'
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}